/* 
    El assert y expect tienen que ir declarados localmente en cada clase (archivo), solamente el should se puede declarar globalmente
    en cualquier archivo dentro de la carpeta test para su uso.
*/

const chai = require('chai')
const should = chai.should();